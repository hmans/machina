# Spawn Swarm

This example keeps the authored scene nearly empty: only the camera and light
live in TOML. A Luau startup system spawns the floor and a large flock of
renderables, then an update system makes the flock orbit, bob, and weave.
